{% extends 'backoffice.html.twig' %}

{% block content %}
  {% include 'backoffice/partials/_breadcrumb.html.twig' with {'path': ['Accueil','Validations/Conformité','Limites']} %}
  <div class="MfCard">
    <h2>Limites Maker-Checker</h2>
    <div class="MfDataTable">
      <table>
        <thead><tr><th>Limite</th><th>Valeur</th><th>Description</th></tr></thead>
        <tbody>
          {% for r in data.data %}
          <tr><td>{{ r.name ?? '' }}</td><td>{{ r.value ?? '' }}</td><td>{{ r.description ?? '' }}</td></tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>
{% endblock %}
