{% extends 'backoffice.html.twig' %}

{% block content %}
  {% include 'backoffice/partials/_breadcrumb.html.twig' with {'path': ['Accueil','Conformité','Inspecteurs']} %}
  <div class="MfCard">
    <h2>Inspecteurs</h2>
    <div class="MfDataTable">
      <table>
        <thead><tr><th>Nom</th><th>Role</th></tr></thead>
        <tbody>
          {% for r in data.data %}
          <tr><td>{{ r.name ?? '' }}</td><td>{{ r.role ?? '' }}</td></tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>
{% endblock %}
